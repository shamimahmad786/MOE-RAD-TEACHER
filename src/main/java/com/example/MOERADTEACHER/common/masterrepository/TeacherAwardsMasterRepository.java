package com.example.MOERADTEACHER.common.masterrepository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.MOERADTEACHER.common.masterbean.TeacherAwardsMaster;
import com.example.MOERADTEACHER.common.masterbean.TeacherQualification;

public interface TeacherAwardsMasterRepository extends JpaRepository<TeacherAwardsMaster, Integer>{

}
